/* Hacer la tabla de multiplicar de un número introducido por pantalla */
const multiplicador = parseInt(
	prompt('Introduce un número para ver su tabla de multiplicar')
);

let multiplicando = 1;
console.log(multiplicador);
console.log('Tabla del', multiplicador);
console.log('---------------------');
console.log(
	`${multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
console.log(
	`${++multiplicando} x ${multiplicador} = ${multiplicando * multiplicador}`
);
